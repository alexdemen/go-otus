syntax = "proto3";

import "event.proto";

option go_package="eventpb";

service EventService {
    rpc Save (Event) returns (Event);
    rpc Remove (Event) returns (bool);
    rpc List (User) returns (EventList);
}

message EventList{
    repeated Event Events = 1;
}

message User{

}

